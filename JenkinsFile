pipeline {
    agent any

    tools {
        maven "local_mvn"
    }

    stages {
        stage('build') {
            steps {
              sh "mvn -version"
              sh "mvn clean install -U"
            }
        }
    }

}