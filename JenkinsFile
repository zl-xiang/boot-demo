pipeline {
    agent any

    tools {
        maven "local_mvn"
        mavenSettingsConfig: "MyGlobalSettings"
    }

    stages {
        stage('build') {
            steps {
              sh "mvn -version"
              sh "mvn clean install"
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}